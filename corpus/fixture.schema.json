{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "deadset conformance rendering manifest",
  "description": "The manifest of one language rendering of a conformance fixture, fixture.json: a section of the Go rendering's txtar archive, a file at the root of the TypeScript rendering's directory, one schema for both. Its first job is the binding the expectation file cannot make: it maps every fixture-local logical name the expectation file uses to the file and the line where this rendering declares that subject, in whatever spelling this language uses. Every name the expectation file uses must appear here, and no other name may, so a manifest never carries a subject the corpus does not test. Its second job is the build matrix, which is a property of one rendering and not of the fixture: a rendering whose files carry build constraints declares the identifiers a run derives from them, and an expectation names configurations from that list. The manifest does not restate the fixture's name or description; those live once, in the expectation file.",
  "type": "object",
  "additionalProperties": false,
  "required": ["symbols"],
  "properties": {
    "configurations": {
      "description": "The build matrix of this rendering, one identifier per configuration, ordered bytewise, each spelled as the operating system, the architecture and any tags joined by hyphens. It is what an expectation's configurations list is drawn from, and it is the matrix a runner configures where the expectation file declares the matrix complete; a language whose analysis has no build matrix declares none. It is present exactly when an expectation of the fixture names configurations or the expectation file declares the matrix complete, and one entry carries meaning under that declaration, because a file no configuration of a one-configuration matrix builds is what the never-built kind reports.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "symbols": {
      "description": "The binding table, keyed by logical name exactly as the expectation file spells it. Each value is the position of that subject in this rendering.",
      "type": "object",
      "minProperties": 1,
      "propertyNames": {
        "pattern": "^[A-Za-z_][A-Za-z0-9_]*(\\.[A-Za-z_][A-Za-z0-9_]*)*$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["file", "line"],
        "properties": {
          "file": {
            "description": "The path of the file that declares the subject, relative to the rendering root, with forward slashes and no empty, dot or dot-dot segment. The rendering root is the archive for Go and the ts/ directory for TypeScript, so target/ and each consumer directory are its first segments.",
            "type": "string",
            "pattern": "^(\\.?[A-Za-z0-9_@-][A-Za-z0-9_@.-]*)(/\\.?[A-Za-z0-9_@-][A-Za-z0-9_@.-]*)*$"
          },
          "line": {
            "description": "The 1-based line of the file on which the subject is declared: the line a finding for it carries in its position.",
            "type": "integer",
            "minimum": 1
          }
        }
      }
    }
  }
}
