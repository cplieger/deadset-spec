# Test vectors

Published input and output vectors for the report merge and for configuration resolution live here, one directory per case under `merge/` and `config/`. Each case holds its inputs and the exact expected output, so a merge or a configuration implementation is tested against declared data rather than against another implementation. The merge algorithm the vectors exercise is the one `contract/grammar/merge.md` states.
